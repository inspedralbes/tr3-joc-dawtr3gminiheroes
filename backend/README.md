# MiniHeroes2D backend

Backend mínimo para cumplir documentación de API (OpenAPI/Swagger) y servir:
- Matchmaking simple (crear sesión + unirse).
- Progresión (XP/oro) en memoria.

## Ejecutar

```bash
cd backend
npm install
npm run dev
```

Por defecto escucha en `http://localhost:3000`.

